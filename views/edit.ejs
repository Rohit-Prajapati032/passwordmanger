<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Form</title>
<link rel="stylesheet" href="/css/edit.css">
</head>
<body>
  <%- include("includes/navbar") %> 
  <div class="form-box">
    <h2>🔐 Edit Account</h2>

    <form action="/edit?_method=PATCH" method="POST">
      <div class="form-group">
        <label>Email</label>
        <input type="email" name="email" placeholder="Enter email" value="<%= password.email %>" required>
      </div>

      <div class="form-group">
        <label>Website Url</label>
        <input type="text" name="website" placeholder="Enter website url" value="<%= password.websiteUrl %>" required>
      </div>

      <div class="form-group">
        <label>Password</label>
        <input type="password" name="password" value="<%=password.password %>" placeholder="Enter password" required>
      </div>

      <input type="hidden" name="id" value="<%= password.id %>">

      <button type="submit" class="btn">Save</button>
    </form>
  </div>

</body>
</html>